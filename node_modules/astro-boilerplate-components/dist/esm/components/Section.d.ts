import type { ReactNode } from 'react';
type ISectionProps = {
    title?: ReactNode;
    children: ReactNode;
};
declare const Section: (props: ISectionProps) => import("react/jsx-runtime").JSX.Element;
export { Section };
